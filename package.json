{
  "name": "schoolos",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@schoolos/web",
    "dev": "npm run dev --workspace=@schoolos/web"
  }
}