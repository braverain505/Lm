{
  "name": "clearis",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@clearis/web",
    "dev": "npm run dev --workspace=@clearis/web"
  },
  "devDependencies": {
    "@types/estree": "^1.0.9",
    "@types/json-schema": "^7.0.15"
  }
}
