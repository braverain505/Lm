# SchoolOS web app — development defaults (copy to .env.local and adjust).

# Where the Next rewrite proxies /api/* to. The FastAPI backend publishes
# under /api, and the browser only ever talks to this single origin so the
# httpOnly auth cookies flow without CORS.
API_URL=http://127.0.0.1:8000